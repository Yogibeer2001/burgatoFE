import Ember from 'ember';

export default Ember.Route.extend({
  model(){
  // // const user = Ember.store.createRecord('user', { email: "", password: ""});
  // // user.save();
  // return this.store.findAll('user');
  }
});
